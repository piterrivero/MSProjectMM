spring:
  application:
    name: genre-service
  cloud:
    config:
      enabled: true
      uri: http://config-service:8000
      
spring:
  profiles: docker
  cloud:
    config:
      uri: http://config-service:8000
