server:
  port: 8080
eureka:
  client:
    fetch-registry: true
    register-with-eureka: true
    serviceUrl:
      default-zone: http://eureka-server-service:8761/eureka
    instance:
      hostname: localhost