server:
  port: 8000
spring:
  cloud: 
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/piterrivero/MSProjectMM
          search-paths: config-data
  application:
    name: config-service
