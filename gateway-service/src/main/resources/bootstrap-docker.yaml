spring:
  application:
    name: gateway-service-docker
  cloud:
    bootstrap:
      name: gateway-service
      yml: file:config-data/gateway-service-docker.yaml
    config:
      enabled: true
      #uri: http://config-service:8888
      uri: http://localhost:8888
      profile: docker
